class UserRoleModel{
    constructor(id, type) {
        this._id = id;
        this._type = type;
    }

    describes(userId) {
        // TODO: return whether this userId is described by the user/role
    }
}