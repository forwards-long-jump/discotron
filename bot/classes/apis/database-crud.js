const database = require(__dirname + "/utils/database-helper.js").getDatabase();


module.exports.update = (table, fields, values, whereFields, whereValues) => {
    // TODO
};

module.exports.insert = (table, fields, values) => {
    // TODO
};

module.exports.delete = (table, whereFields, whereValues) => {
    // TODO
};

module.exports.select = (fields, table, whereFields = [], whereValues = []) => {
    // TODO
};