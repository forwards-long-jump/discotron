{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Discotron",
            "program": "${workspaceFolder}/index.js",
            "cwd": "${workspaceFolder}/",
            "stopOnEntry": false
        }
    ]
}